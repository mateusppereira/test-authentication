// import { all, takeLatest } from "redux-saga/effects";
import { all } from "redux-saga/effects";

export default function* root() {
  yield all([
    // takeLatest(xxxxxx, xxxxxxxx)
  ]);
}
